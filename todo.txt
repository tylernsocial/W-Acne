Instead of a full clear skin class, you could handle this with a confidence threshold in your app logic:

If the model's top prediction is below say 70% confidence → show a "we couldn't confidently classify this" message
Only show a class label when confidence is high enough

safety net without needing to collect clear skin images.